# 데이터 흐름 및 상태 관리 문서

## 📋 목차
1. [데이터 흐름 개요](#데이터-흐름-개요)
2. [상태 관리 전략](#상태-관리-전략)
3. [이벤트 처리 흐름](#이벤트-처리-흐름)
4. [API 통신](#api-통신)
5. [폼 데이터 처리](#폼-데이터-처리)

---

## 데이터 흐름 개요

### 단방향 데이터 흐름 (Unidirectional Data Flow)

```
┌─────────────────────────────────────────────────────────┐
│                     App (Root)                          │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │              MainLayout                           │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  Navbar                                     │ │ │
│  │  │  - isScrolled (local state)                 │ │ │
│  │  │  - isMobileMenuOpen (local state)           │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  Hero                                       │ │ │
│  │  │  - 정적 콘텐츠                               │ │ │
│  │  │  - 스크롤 함수 (이벤트 핸들러)               │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  ServiceOverview                            │ │ │
│  │  │  - services (정적 데이터)                    │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  PortfolioGrid                              │ │ │
│  │  │  - activeFilter (local state)               │ │ │
│  │  │  - portfolioData (imported data)            │ │ │
│  │  │    │                                         │ │ │
│  │  │    └──> PortfolioCard (props)               │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  ContactForm                                │ │ │
│  │  │  - form state (react-hook-form)             │ │ │
│  │  │  - validation (zod)                         │ │ │
│  │  │  - Supabase API call                        │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  Footer                                     │ │ │
│  │  │  - 정적 콘텐츠                               │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 데이터 소스 분류

#### 1. 정적 데이터 (Static Data)
**위치**: `src/data/`, `src/constants/`

```typescript
// src/constants/index.ts
export const NAV_LINKS = [
    { name: 'Home', href: '#home' },
    { name: 'Services', href: '#services' },
    // ...
] as const;

export const CONTACT_INFO = {
    email: 'highflood.video@gmail.com',
    phone: '010-6567-6625',
    address: '경상북도 포항시',
} as const;
```

**사용 위치**:
- Navbar: `NAV_LINKS`
- Footer: `FOOTER_LINKS`, `CONTACT_INFO`, `COMPANY_INFO`
- ServiceOverview: `services` (컴포넌트 내부 정의)

#### 2. 동적 데이터 (Dynamic Data)
**위치**: `src/data/portfolio.ts`

```typescript
// src/data/portfolio.ts
export const portfolioData: PortfolioItem[] = [
    {
        id: 1,
        title: 'MES 시스템',
        category: 'System',
        tech: ['React', 'FastAPI', 'PostgreSQL'],
        description: '제조 현장 통합 관리 시스템',
        image: 'https://...',
        link: 'https://...'
    },
    // ...
];
```

**사용 위치**:
- PortfolioGrid: 포트폴리오 데이터 표시

#### 3. 사용자 입력 데이터 (User Input)
**위치**: ContactForm 컴포넌트

```typescript
interface ContactFormData {
    name: string;
    company: string;
    phone: string;
    email: string;
    projectType: string;
    budget: string;
    description: string;
    privacyConsent: boolean;
}
```

**흐름**:
```
사용자 입력
  ↓
React Hook Form (상태 관리)
  ↓
Zod 유효성 검증
  ↓
Supabase API 호출
  ↓
데이터베이스 저장
```

---

## 상태 관리 전략

### 로컬 상태 (Local State)

#### 1. Navbar 컴포넌트

```tsx
export default function Navbar() {
    // 스크롤 상태
    const [isScrolled, setIsScrolled] = useState(false);
    
    // 모바일 메뉴 상태
    const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

    useEffect(() => {
        const handleScroll = () => {
            setIsScrolled(window.scrollY > 50);
        };
        window.addEventListener('scroll', handleScroll);
        return () => window.removeEventListener('scroll', handleScroll);
    }, []);
    
    // ...
}
```

**상태 업데이트 흐름**:
```
1. 사용자가 페이지 스크롤
   ↓
2. window 'scroll' 이벤트 발생
   ↓
3. handleScroll 함수 실행
   ↓
4. setIsScrolled(true/false) 호출
   ↓
5. React가 컴포넌트 리렌더링
   ↓
6. 조건부 className 적용
   ↓
7. 네비게이션 스타일 변경 (배경색, 그림자)
```

#### 2. PortfolioGrid 컴포넌트

```tsx
export default function PortfolioGrid() {
    const [activeFilter, setActiveFilter] = useState<string>('All');

    const filteredProjects = portfolioData.filter(
        (item) => activeFilter === 'All' || item.category === activeFilter
    );

    return (
        <>
            {/* 필터 버튼 */}
            {['All', 'Web', 'System', 'Dashboard'].map((filter) => (
                <button
                    key={filter}
                    onClick={() => setActiveFilter(filter)}
                    className={activeFilter === filter ? 'active' : ''}
                >
                    {filter}
                </button>
            ))}

            {/* 필터링된 프로젝트 */}
            {filteredProjects.map((project) => (
                <PortfolioCard key={project.id} {...project} />
            ))}
        </>
    );
}
```

**상태 업데이트 흐름**:
```
1. 사용자가 필터 버튼 클릭 (예: 'Web')
   ↓
2. onClick 이벤트 발생
   ↓
3. setActiveFilter('Web') 호출
   ↓
4. activeFilter 상태 업데이트
   ↓
5. 컴포넌트 리렌더링
   ↓
6. filteredProjects 재계산
   ↓
7. 'Web' 카테고리 프로젝트만 표시
```

#### 3. ContactForm 컴포넌트 (React Hook Form)

```tsx
export default function ContactForm() {
    const form = useForm<ContactFormData>({
        resolver: zodResolver(contactSchema),
        defaultValues: {
            name: '',
            company: '',
            // ...
        },
    });

    const onSubmit = async (data: ContactFormData) => {
        // Supabase에 데이터 저장
    };

    return (
        <form onSubmit={form.handleSubmit(onSubmit)}>
            <Input {...form.register('name')} />
            {/* ... */}
        </form>
    );
}
```

**폼 상태 관리 흐름**:
```
1. 사용자가 입력 필드에 타이핑
   ↓
2. form.register('name')이 onChange 이벤트 감지
   ↓
3. React Hook Form이 내부 상태 업데이트
   ↓
4. 실시간 유효성 검증 (Zod)
   ↓
5. 에러 메시지 표시 (있는 경우)
   ↓
6. 사용자가 제출 버튼 클릭
   ↓
7. form.handleSubmit(onSubmit) 실행
   ↓
8. 최종 유효성 검증
   ↓
9. 통과 시 onSubmit 함수 호출
   ↓
10. Supabase API 호출
```

---

## 이벤트 처리 흐름

### 1. 스크롤 이벤트

#### Navbar 스크롤 감지
```tsx
useEffect(() => {
    const handleScroll = () => {
        setIsScrolled(window.scrollY > 50);
    };
    
    window.addEventListener('scroll', handleScroll);
    
    // 클린업 함수
    return () => window.removeEventListener('scroll', handleScroll);
}, []);
```

**이벤트 흐름**:
```
페이지 로드
  ↓
useEffect 실행 (마운트 시)
  ↓
scroll 이벤트 리스너 등록
  ↓
사용자가 스크롤
  ↓
handleScroll 함수 실행
  ↓
window.scrollY 값 확인
  ↓
50px 초과 시 setIsScrolled(true)
  ↓
컴포넌트 리렌더링
  ↓
네비게이션 배경색 변경
```

**클린업**:
```
컴포넌트 언마운트
  ↓
useEffect의 return 함수 실행
  ↓
이벤트 리스너 제거
  ↓
메모리 누수 방지
```

### 2. 클릭 이벤트

#### 스무스 스크롤 네비게이션
```tsx
const scrollToSection = (href: string) => {
    setIsMobileMenuOpen(false);  // 모바일 메뉴 닫기
    const element = document.querySelector(href);
    if (element) {
        element.scrollIntoView({ behavior: 'smooth' });
    }
};

// 사용
<a
    href="#services"
    onClick={(e) => {
        e.preventDefault();  // 기본 동작 방지
        scrollToSection('#services');
    }}
>
    Services
</a>
```

**이벤트 흐름**:
```
1. 사용자가 'Services' 링크 클릭
   ↓
2. onClick 이벤트 발생
   ↓
3. e.preventDefault() 실행 (페이지 이동 방지)
   ↓
4. scrollToSection('#services') 호출
   ↓
5. setIsMobileMenuOpen(false) 실행 (모바일 메뉴 닫기)
   ↓
6. document.querySelector('#services') 실행
   ↓
7. 요소 찾기 성공
   ↓
8. element.scrollIntoView({ behavior: 'smooth' }) 실행
   ↓
9. 브라우저가 부드럽게 스크롤
   ↓
10. Services 섹션으로 이동
```

### 3. 폼 제출 이벤트

```tsx
const onSubmit = async (data: ContactFormData) => {
    try {
        const { error } = await supabase
            .from('contacts')
            .insert([{
                ...data,
                created_at: new Date().toISOString(),
            }]);

        if (error) throw error;

        // 성공 처리
        alert('문의가 성공적으로 접수되었습니다!');
        form.reset();
    } catch (error) {
        console.error('Error:', error);
        alert('문의 접수 중 오류가 발생했습니다.');
    }
};

<form onSubmit={form.handleSubmit(onSubmit)}>
```

**이벤트 흐름**:
```
1. 사용자가 폼 작성
   ↓
2. 제출 버튼 클릭
   ↓
3. form의 onSubmit 이벤트 발생
   ↓
4. form.handleSubmit(onSubmit) 실행
   ↓
5. React Hook Form이 유효성 검증
   ↓
6. Zod 스키마로 데이터 검증
   ↓
7. 검증 실패 시 → 에러 메시지 표시 → 종료
   ↓
8. 검증 통과 시 → onSubmit 함수 호출
   ↓
9. Supabase API 호출 (비동기)
   ↓
10. 데이터베이스에 저장
   ↓
11. 성공 시 → 알림 표시 + 폼 리셋
    실패 시 → 에러 메시지 표시
```

---

## API 통신

### Supabase 설정

**파일**: `src/lib/supabase.ts`

```typescript
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

### ContactForm에서의 사용

```tsx
import { supabase } from '../../lib/supabase';

const onSubmit = async (data: ContactFormData) => {
    const { data: result, error } = await supabase
        .from('contacts')
        .insert([{
            name: data.name,
            company: data.company,
            phone: data.phone,
            email: data.email,
            project_type: data.projectType,
            budget: data.budget,
            description: data.description,
            privacy_consent: data.privacyConsent,
            created_at: new Date().toISOString(),
        }]);

    if (error) {
        console.error('Supabase error:', error);
        throw error;
    }

    return result;
};
```

### API 호출 흐름

```
ContactForm 컴포넌트
  ↓
onSubmit 함수 실행
  ↓
supabase.from('contacts').insert([...])
  ↓
Supabase Client (JavaScript SDK)
  ↓
HTTP POST 요청
  ↓
Supabase API (https://xxx.supabase.co)
  ↓
PostgreSQL 데이터베이스
  ↓
응답 반환 (성공/실패)
  ↓
ContactForm에서 처리
  ↓
사용자에게 피드백 표시
```

---

## 폼 데이터 처리

### 1. 폼 스키마 정의 (Zod)

```typescript
import { z } from 'zod';

const contactSchema = z.object({
    name: z.string()
        .min(2, '이름은 최소 2자 이상이어야 합니다')
        .max(50, '이름은 최대 50자까지 입력 가능합니다'),
    
    company: z.string()
        .min(2, '회사명을 입력해주세요'),
    
    phone: z.string()
        .regex(/^01[0-9]-?[0-9]{4}-?[0-9]{4}$/, '올바른 전화번호 형식이 아닙니다'),
    
    email: z.string()
        .email('올바른 이메일 주소를 입력해주세요'),
    
    projectType: z.string()
        .min(1, '프로젝트 유형을 선택해주세요'),
    
    budget: z.string()
        .min(1, '예산을 선택해주세요'),
    
    description: z.string()
        .min(10, '프로젝트 설명은 최소 10자 이상 입력해주세요')
        .max(1000, '프로젝트 설명은 최대 1000자까지 입력 가능합니다'),
    
    privacyConsent: z.boolean()
        .refine((val) => val === true, {
            message: '개인정보 수집 및 이용에 동의해주세요',
        }),
});

type ContactFormData = z.infer<typeof contactSchema>;
```

### 2. React Hook Form 설정

```tsx
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';

const form = useForm<ContactFormData>({
    resolver: zodResolver(contactSchema),
    mode: 'onBlur',  // 포커스 아웃 시 검증
    defaultValues: {
        name: '',
        company: '',
        phone: '',
        email: '',
        projectType: '',
        budget: '',
        description: '',
        privacyConsent: false,
    },
});
```

### 3. 폼 필드 등록

```tsx
<Input
    {...form.register('name')}
    label="이름"
    placeholder="홍길동"
    error={form.formState.errors.name?.message}
/>
```

**register 함수의 역할**:
```typescript
form.register('name')
// 반환값:
{
    name: 'name',
    onChange: (e) => { /* 상태 업데이트 */ },
    onBlur: (e) => { /* 유효성 검증 */ },
    ref: (el) => { /* DOM 참조 */ }
}
```

### 4. 실시간 유효성 검증

```
사용자가 입력 필드에 타이핑
  ↓
onChange 이벤트 발생
  ↓
React Hook Form이 값 업데이트
  ↓
사용자가 다른 필드로 이동 (blur)
  ↓
onBlur 이벤트 발생
  ↓
Zod 스키마로 유효성 검증
  ↓
검증 실패 시
  ↓
form.formState.errors 업데이트
  ↓
에러 메시지 표시
```

### 5. 제출 처리

```tsx
const onSubmit = async (data: ContactFormData) => {
    try {
        // 1. 로딩 상태 표시 (선택)
        setIsSubmitting(true);

        // 2. API 호출
        const { error } = await supabase
            .from('contacts')
            .insert([{
                ...data,
                created_at: new Date().toISOString(),
            }]);

        // 3. 에러 처리
        if (error) throw error;

        // 4. 성공 처리
        alert('문의가 성공적으로 접수되었습니다!');
        form.reset();  // 폼 초기화

    } catch (error) {
        // 5. 실패 처리
        console.error('Error:', error);
        alert('문의 접수 중 오류가 발생했습니다.');
    } finally {
        // 6. 로딩 상태 해제
        setIsSubmitting(false);
    }
};
```

---

## 데이터 흐름 다이어그램

### 전체 데이터 흐름

```
┌──────────────────────────────────────────────────────────────┐
│                        사용자 상호작용                         │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│                        이벤트 발생                            │
│  - 클릭, 스크롤, 입력, 제출 등                                 │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│                      이벤트 핸들러 실행                        │
│  - onClick, onScroll, onChange, onSubmit                     │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│                        상태 업데이트                          │
│  - useState, React Hook Form                                 │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│                      컴포넌트 리렌더링                         │
│  - React가 가상 DOM 재계산                                     │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│                        DOM 업데이트                           │
│  - 변경된 부분만 실제 DOM에 반영                               │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│                        화면 업데이트                          │
│  - 브라우저가 변경사항 렌더링                                  │
└──────────────────────────────────────────────────────────────┘
```

---

## 요약

### 데이터 소스
1. **정적 데이터**: constants, 컴포넌트 내부 정의
2. **동적 데이터**: data/portfolio.ts
3. **사용자 입력**: ContactForm

### 상태 관리
1. **로컬 상태**: useState (Navbar, PortfolioGrid)
2. **폼 상태**: React Hook Form (ContactForm)
3. **전역 상태**: 현재 사용 안 함 (필요시 Context API, Zustand 등)

### 이벤트 처리
1. **스크롤**: useEffect + window.addEventListener
2. **클릭**: onClick + scrollToSection
3. **폼 제출**: onSubmit + API 호출

### API 통신
1. **Supabase**: PostgreSQL 데이터베이스
2. **비동기 처리**: async/await
3. **에러 처리**: try/catch

### 데이터 검증
1. **Zod**: 스키마 기반 유효성 검증
2. **React Hook Form**: 폼 상태 관리 + 검증 통합
